`sync.sh` is a configurable shell script aimed to provide synchronization of local directories through `rsync`
([link](https://github.com/WayneD/rsync)), with `git` ([link](https://git-scm.com/)) integration.
